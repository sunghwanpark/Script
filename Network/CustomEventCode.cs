using UnityEngine;

namespace SHProject.Network
{
    public sealed class CustomEventCode
    {
        public const byte FirstLocate = 0;
    }

    public sealed class CustomParameterCode
    {
        public const byte Locate = 0;
        public const byte FirstTurn = 1;
    }
}